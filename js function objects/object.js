var student = {
    id: 14, 
    name: 'Latifa sikder bristy',
    class: 9,
    marks: 98
}

var mobile = {
    brand : 'Samsung',
    price : 40,
    storage : '64gb',
    camera : '50MP',
}

var monitor = {
    brand : 'HP',
    price : 29000,
    retailer : 'Rayan',
}

// console.log(monitor);
console.log(monitor.price);

/* const obj = {
    "key1": "value1",
    "key2": "value2",
};

// Get the keys of the object
const keys = Object.keys(obj);
console.log(keys); // ["key1", "key2"]

// Get the values of the object
const values = Object.values(obj);
console.log(values); // ["value1", "value2"] */